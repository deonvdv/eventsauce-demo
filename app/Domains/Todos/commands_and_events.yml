namespace: Todos
commands:
  # AddTodo:
  #   fields:
  #     identifier: TodoId
  #     user_id: int
  #     todo: string
  ProcessTodo:
    fields:
      id: TodoId
      todo: string
events:
  TodoAdded:
    fields:
      id: TodoId
      user_id: int
      todo: string
  TodoUpdated:
    fields:
      id: TodoId
      todo: string
